{
  "name": "@dropitx/cli",
  "version": "0.2.0",
  "description": "CLI for publishing files to shareable URLs via DropItX",
  "type": "module",
  "bin": {
    "dropitx": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist"
  ]
}
