{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "workspace": "packages/*",
  "buildCommand": "pnpm --filter @dropitx/api build",
  "outputDirectory": "packages/api/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
